# AGENTS.md — このリポジトリを編集するAI・開発者へ

左右分割型ワイヤレス・オーソリニアキーボード「mokumoku-keeb(仮称)」の設計リポジトリです。
編集前に必ず以下のルールに従ってください。

## 情報の優先順位

1. **仕様の一次情報は `docs/product-requirements.md`** です。他のドキュメントやコードと矛盾した場合、product-requirements.md が正です(product-requirements.md 自体の変更はユーザーの合意を得ること)。
2. 設計判断は `docs/decisions/` にADRとして記録します。判断を覆す場合は新しいADRを追加し、古いADRのStatusをSupersededに変更します。
3. 外部設計(FelixKeeb、Helix、Corne等)を参考にした場合は、**出典とライセンスを `docs/references.md` に必ず記録**してください。外部設計の無断複製は禁止です。

## 整合性ルール

- **ピン割り当て(GPIO)を変更した場合は、回路図・`docs/electrical.md` のGPIO表・ZMK設定(`firmware/zmk/`)を同時に更新**すること。片方だけの更新は禁止。
- キー配列を変更した場合は `docs/key-layout.md`・回路図マトリクス・ZMKキーマップを同時に更新すること。
- 右半分は左半分の**完全な左右反転**です。左半分を基準に設計し、ミラー規則は `docs/key-layout.md` に従うこと。

## ファイル配置ルール

- KiCadの**編集可能なソース**(.kicad_pro / .kicad_sch / .kicad_pcb、プロジェクト固有シンボル・フットプリント)は `hardware/kicad/` に置く。
- **Gerber・ドリル・BOM・座標データなどの生成物は `production/<revision>/`(例: `production/rev-a/`)以外に置かない**。ソースと生成物を混在させないこと。
- `production/<revision>/` に置いたデータは発注時点のスナップショットとして凍結し、以後編集しない。修正は次リビジョンとして作成する。

## 検証ルール

- **未検証のフットプリントを製造用に使用しない**。フットプリントは実部品のデータシートと照合し、検証状態を記録してから使用すること。
- 製造候補とする前に、KiCadの **ERC / DRC を通す**こと。
- 発注前に以下を必ず確認すること:
  - **JSTコネクタの極性**(バッテリー側と基板側の両方)
  - **ダイオードの方向**(マトリクス全体で一貫していること)
  - **左右反転の整合性**(右半分のミラーが正しいこと)
  - **Bluetoothアンテナ禁止領域**(銅箔・金属・バッテリー・ねじの非配置)
- `docs/verification-checklist.md` の製造前チェックリストが完了するまで、**「production-ready」「製造可能」「検証済み」等の表現を使用しない**。

## 不確実性の扱い

- 未確認の寸法・ピン配置・極性・フットプリントを**推測で確定しない**。未確定事項は `docs/product-requirements.md` の未確定事項リストおよび該当ドキュメントに明記する。
- 公式資料で確認できていない内容を「確認済み」と記載しない。

## 禁止事項(ユーザーの明示的な指示がない限り)

- 部品や基板の**発注・購入**
- 外部サービスへの**アップロード**(製造業者サイト含む)
- **git commit / push**
